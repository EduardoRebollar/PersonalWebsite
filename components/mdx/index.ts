/**
 * MDX component map — custom React components callable from any MDX file as
 * JSX (<Figure src="…" />, etc.). Consumed by mdx-components.tsx at the
 * project root, which Next.js auto-loads for every MDX page.
 */
export { Figure } from './Figure';
export { Aside } from './Aside';
export { TechStack } from './TechStack';
